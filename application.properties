<app-navbar></app-navbar>
<div class="container " style="margin-top: 150px;">
  <div class="card p-4 shadow">
    <h3 class="mb-3">Pay Fine</h3>
 
    <div class="mb-3">
      <label class="form-label">Member ID</label>
      <input type="number" class="form-control" [(ngModel)]="memberId" required />
    </div>
 
    <div class="mb-3">
      <label class="form-label">Fine ID</label>
      <input type="number" class="form-control" [(ngModel)]="fineId" required />  
    </div>
 
    <button class="btn btn-success" (click)="payFine()">Pay Fine</button>
 
    <div *ngIf="message" class="alert alert-info mt-3">
      {{ message }}
    </div>
  </div>
</div>
 
import { Component } from '@angular/core';
import { FinesService } from '../fines.service';
import { NavbarComponent } from '../navbar/navbar.component';
import { RouterLink } from '@angular/router';
import { FormsModule } from '@angular/forms';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-fine-pay',
  imports: [NavbarComponent,RouterLink,FormsModule, CommonModule],
  templateUrl: './fine-pay.component.html',
  styleUrl: './fine-pay.component.css'
})
export class FinePayComponent {
  memberId!: number;
  fineId!: number;
  message: string = '';
 
  constructor(private fineService: FinesService) {}
 
  payFine() {
    this.fineService.payFine(this.memberId, this.fineId).subscribe({
      next: () => {
        this.message = `Fine with ID ${this.fineId} paid successfully by member ${this.memberId}.`;
        console.log(this.message);
      },
      error: (err) => {
        this.message = 'Error paying fine. Please check the Member ID and Fine ID.';
        console.error(err);
      },
    });
  }
}

<app-navbar></app-navbar>
<div class="container " style="margin-top: 150px;">
  <div class="card p-4 shadow-sm">
    <h3 class="mb-4">Calculate Fine</h3>
 
    <div class="mb-3">
      <label for="memberId" class="form-label">Enter Member ID</label>
      <input type="number" class="form-control" id="memberId"[(ngModel)]="memberId" placeholder="e.g. 3"  min="1" required />
    </div>
 
    <button class="btn btn-primary mb-3" (click)="calculateFine()">Calculate</button>
 
    <div *ngIf="message" class="alert alert-success mt-2">{{ message }}</div>
    <div *ngIf="error" class="alert alert-danger mt-2">{{ error }}</div>
 
    <div *ngIf="fineDetails" class="card mt-3 p-3 bg-light border">
      <h5>Fine Details</h5>
      <p><strong>Fine ID:</strong> {{ fineDetails.fineId }}</p>
      <p><strong>Status:</strong> {{ fineDetails.status }}</p>
      <p><strong>Amount:</strong> ₹{{ fineDetails.amount }}</p>
      <p><strong>Transaction Date:</strong> {{ fineDetails.transactionDate }}</p>
 
      <h6 class="mt-3">Member Details</h6>
      <p><strong>Name:</strong> {{ fineDetails.memberId.name }}</p>
      <p><strong>Email:</strong> {{ fineDetails.memberId.email }}</p>
      <p><strong>Phone:</strong> {{ fineDetails.memberId.phone }}</p>
      <p><strong>Address:</strong> {{ fineDetails.memberId.address }}</p>
      <p><strong>Status:</strong> {{ fineDetails.memberId.membershipStatus }}</p>
    </div>
  </div>
</div>

import { Component } from '@angular/core';
import { NavbarComponent } from '../navbar/navbar.component';
import { RouterLink } from '@angular/router';
import { FinesService } from '../fines.service';
import { FormsModule } from '@angular/forms';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-fine-cal',
  imports: [NavbarComponent,RouterLink,FormsModule,CommonModule],
  templateUrl: './fine-cal.component.html',
  styleUrl: './fine-cal.component.css'
})
export class FineCalComponent {
  memberId!: number;
  fineDetails: any = null;
  message = '';
  error = '';
 
  constructor(private finesService: FinesService) {}
 
  calculateFine() {
    this.message = '';
    this.error = '';
    this.fineDetails = null;
 
    if (!this.memberId || this.memberId <= 0) {
      this.error = 'Please enter a valid Member ID.';
      return;
    }
 
    this.finesService.calculateFine(this.memberId).subscribe({
      next: (res) => {
        this.fineDetails = res;
        this.message = 'Fine calculated successfully.';
        console.log(res);
      },
      error: (err) => {
        this.error = 'Failed to calculate fine. Please check the Member ID.';
        console.error(err);
      },
    });
  }
}

import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
 
@Injectable({
  providedIn: 'root',
})
export class FinesService {
private pathCal = 'http://localhost:9090/fines/calculatefine'; // POST /{memberId}
private pathPay = 'http://localhost:9090/fines/pay'; // POST /{memberId}/{fineId}
private pathView = 'http://localhost:9090/fines/allfines'; // GET
 
  constructor(private http: HttpClient) {}
 
  calculateFine(memberId: number): Observable<any> {
return this.http.post(`${this.pathCal}/${memberId}`, {});
  }
 
  payFine(memberId: number, fineId: number): Observable<any> {
return this.http.post(`${this.pathPay}/${memberId}/${fineId}`, {});
  }
 
  getAllFines(): Observable<any[]> {
    return this.http.get<any[]>(this.pathView);
  }
}

according to the codes you gave enhance the above angular with good ui

