<app-navbar></app-navbar>

<div class="container" style="margin-top: 100px; max-width: 600px;">
  <div class="card shadow rounded-4">
    <div class="card-header bg-primary text-white text-center rounded-top-4">
      <h3 class="mb-0">Register</h3>
    </div>
    <div class="card-body">
      <form (ngSubmit)="validateR(registerForm)" #registerForm="ngForm" novalidate>

        <!-- Name -->
        <div class="mb-3">
          <label for="name" class="form-label fw-semibold">Full Name</label>
          <input ngModel name="name" required type="text" class="form-control rounded-3" placeholder="Enter your name" />
        </div>

        <!-- Email -->
        <div class="mb-3">
          <label for="email" class="form-label fw-semibold">Email</label>
          <input ngModel name="email" required type="email" class="form-control rounded-3" placeholder="Enter your email" />
        </div>

        <!-- Password -->
        <div class="mb-3">
          <label for="password" class="form-label fw-semibold">Password</label>
          <input ngModel name="password" required type="password" class="form-control rounded-3" placeholder="Create password" />
        </div>

        <!-- Phone -->
        <div class="mb-3">
          <label for="phone" class="form-label fw-semibold">Phone</label>
          <input ngModel name="phone" required type="text" class="form-control rounded-3" placeholder="Enter phone number" />
        </div>

        <!-- Address -->
        <div class="mb-3">
          <label for="address" class="form-label fw-semibold">Address</label>
          <input ngModel name="address" required type="text" class="form-control rounded-3" placeholder="Enter address" />
        </div>

        <!-- Role -->
        <div class="mb-3">
          <label for="role" class="form-label fw-semibold">Select Role</label>
          <select ngModel name="role" required class="form-select rounded-3" [(ngModel)]="selectedRole">
            <option value="" disabled selected>Select a role</option>
            <option value="member">Member</option>
            <option value="admin">Admin</option>
          </select>
        </div>

        <button class="btn btn-success w-100 rounded-3 fw-bold" type="submit" [disabled]="registerForm.invalid">
          Register
        </button>

        <div class="text-center mt-3">
          <small>Already have an account? <a routerLink="/login">Login here</a></small>
        </div>
      </form>
    </div>
  </div>
</div>
