<app-navbar></app-navbar>

<div class="container" style="margin-top: 100px;">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg rounded-4">
        <div class="card-header bg-primary text-white text-center rounded-top-4">
          <h3 class="mb-0">Register</h3>
        </div>
        <div class="card-body">
          <form #registerForm="ngForm" (ngSubmit)="validateR(registerForm)" novalidate>

            <!-- Username -->
            <div class="mb-3">
              <label for="username" class="form-label">Username</label>
              <input ngModel name="username" id="username" required class="form-control" placeholder="Enter username">
            </div>

            <!-- Email -->
            <div class="mb-3">
              <label for="email" class="form-label">Email</label>
              <input ngModel type="email" name="email" id="email" required class="form-control" placeholder="Enter email">
            </div>

            <!-- Password -->
            <div class="mb-3">
              <label for="password" class="form-label">Password</label>
              <input ngModel type="password" name="password" id="password" required class="form-control" placeholder="Enter password">
            </div>

            <!-- Phone (only if role is Member) -->
            <div class="mb-3" *ngIf="selectedRole === 'member'">
              <label for="phone" class="form-label">Phone</label>
              <input ngModel name="phone" id="phone" class="form-control" placeholder="Enter phone number">
            </div>

            <!-- Address (only if role is Member) -->
            <div class="mb-3" *ngIf="selectedRole === 'member'">
              <label for="address" class="form-label">Address</label>
              <input ngModel name="address" id="address" class="form-control" placeholder="Enter address">
            </div>

            <!-- Role -->
            <div class="mb-3">
              <label for="role" class="form-label">Role</label>
              <select ngModel name="role" [(ngModel)]="selectedRole" id="role" required class="form-control">
                <option value="" disabled selected>Select role</option>
                <option value="member">Member</option>
                <option value="admin">Admin</option>
              </select>
            </div>

            <button type="submit" class="btn btn-success w-100 mt-3" [disabled]="registerForm.invalid">
              Register
            </button>

            <div class="text-center mt-3">
              <small>Already have an account? <a routerLink="/login">Login</a></small>
            </div>

          </form>
        </div>
      </div>
    </div>
  </div>
</div>
