<app-navbar></app-navbar>

<div class="register-container d-flex justify-content-center align-items-center min-vh-100">
  <div class="card shadow-lg w-75">
    <div class="row g-0">
      
      <!-- Left side image -->
      <div class="col-md-6 d-none d-md-block">
        <img src="https://images.unsplash.com/photo-1521791136064-7986c2920216" class="img-fluid h-100" alt="Register" style="object-fit: cover;">
      </div>

      <!-- Right side form -->
      <div class="col-md-6 bg-light p-5">
        <h3 class="text-center mb-4">Register</h3>
        <form (ngSubmit)="register()" #registerForm="ngForm" novalidate>

          <!-- Name -->
          <div class="mb-3 input-group">
            <span class="input-group-text"><i class="fas fa-user"></i></span>
            <input type="text" name="name" class="form-control" [(ngModel)]="formData.name" placeholder="Full Name" required />
          </div>

          <!-- Email -->
          <div class="mb-3 input-group">
            <span class="input-group-text"><i class="fas fa-envelope"></i></span>
            <input type="email" name="email" class="form-control" [(ngModel)]="formData.email" placeholder="Email" required />
          </div>

          <!-- Phone -->
          <div class="mb-3 input-group">
            <span class="input-group-text"><i class="fas fa-phone"></i></span>
            <input type="text" name="phone" class="form-control" [(ngModel)]="formData.phone" placeholder="Phone" required />
          </div>

          <!-- Address -->
          <div class="mb-3 input-group">
            <span class="input-group-text"><i class="fas fa-map-marker-alt"></i></span>
            <input type="text" name="address" class="form-control" [(ngModel)]="formData.address" placeholder="Address" required />
          </div>

          <!-- Password -->
          <div class="mb-3 input-group">
            <span class="input-group-text"><i class="fas fa-lock"></i></span>
            <input type="password" name="password" class="form-control" [(ngModel)]="formData.password" placeholder="Password" required />
          </div>

          <!-- Role selection -->
          <div class="mb-3 input-group">
            <span class="input-group-text"><i class="fas fa-user-tag"></i></span>
            <select class="form-select" name="role" [(ngModel)]="selectedRole" required>
              <option value="" disabled selected>Select Role</option>
              <option value="member">Member</option>
              <option value="admin">Admin</option>
            </select>
          </div>

          <!-- Submit button -->
          <div class="d-grid">
            <button class="btn btn-primary" type="submit" [disabled]="registerForm.invalid">
              <i class="fas fa-user-plus me-2"></i> Register
            </button>
          </div>

        </form>
        <p class="text-center mt-3">Already have an account? <a routerLink="/login">Login</a></p>
      </div>
    </div>
  </div>
</div>


.register-container {
  background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
  padding: 40px 0;
}

.card {
  border-radius: 15px;
}

input.ng-invalid.ng-touched,
select.ng-invalid.ng-touched {
  border-color: red;
}

